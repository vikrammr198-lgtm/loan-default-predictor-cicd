apiVersion: v1
kind: Service
metadata:
  name: loan-default-predictor-service
spec:
  selector:
    app: loan-default-predictor
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort
